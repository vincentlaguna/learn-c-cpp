#include <iostream>

// row ->  |--|--|--|
//         |--|--|--|
//         |--|--|--|
//         |--|--|--|

template <typename T = int>

class matrix
{
private:
  size_t mRows, mCols;
  std::vector<std::vector<T> *> rows;

public:
  matrix(const size_t &rows, const size_t &cols)
  {
    
  }
};

int main(void)
{
  
  return(0);
}