#include <stdio.h>
#include <stdlib.h>
#include <time.h>

#define SIZE 10

int compare(const void *a, const void *b);

int main(void)
{
  int numbers[SIZE];
  int x;
  srand((unsigned)time(NULL));
  // Populate the array
  return 0;
}

int compare(const void *a, const void *b)
{
  
}