#include <stdio.h>
#include <stdlib.h>

void get_input(char **b);

// Create the array of pointers and output the results
int main(void)
{
  char *names[5];
  int x;
  
  for(x = 0; x < 5; x++)
  {
    
  }
  return(0);
}

void get_input(char **b)
{
  
}