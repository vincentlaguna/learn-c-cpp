#include <stdio.h>
#include <stdlib.h>

int sum(int x, int y, int z)
{
  char c = 2;
  int *a = NULL;
  
  #ifdef DEBUG
    fprintf(stderr, " = %d\n", );
    fprintf(stderr, " = %d\n", );
    fprintf(stderr, " = %d\n", );
    fprintf(stderr, " = %d\n", );
}