#include <stdio.h>
#include <stdlib.h>
#include <string.h>

struct person
{
  char name[32];
  int age;
  float iq;
};

struct person *allocateStruct(void);
struct person fetchStruct(struct person *p);
void showStruct(struct person *p);

int main(void)
{
  
  return(0);
}

struct person *allocateStruct(void)
{
  
}

struct person fetchStruct(struct person *p)
{
  
}

void showStruct(struct person *p)
{
  
}